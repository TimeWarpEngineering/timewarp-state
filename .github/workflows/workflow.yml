name: CI/CD Pipeline

on:
  pull_request:
    paths:
      - 'source/**'
      - 'tests/**'
      - 'scripts/**'
      - '.github/workflows/workflow.yml'
      - '*.props'
      - '*.targets'
      - 'Directory.Build.props'
      - 'global.json'
  push:
    branches:
      - master
    paths:
      - 'source/**'
      - 'tests/**'
      - 'scripts/**'
      - '.github/workflows/workflow.yml'
      - 'Directory.Build.props'
      - '*.props'
      - '*.targets'
      - 'global.json'
      - 'Documentation/**'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      mode:
        description: 'Pipeline mode. merge = no publish (default). release = break-glass publish; also requires confirm. probe = trusted-publishing check only (OIDC login, no build, no publish).'
        type: choice
        options:
          - merge
          - release
          - probe
        default: merge
      confirm:
        description: 'Break-glass guard: type the word release to confirm publishing when mode is release. Ignored otherwise.'
        type: string
        default: ''

env:
  DOTNET_NOLOGO: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  # CI/CD job for PR and push events
  ci:
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && github.ref == 'refs/heads/master')
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: pwsh
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      # nuget.config lists artifacts/packages as a local source; a fresh checkout has no such folder and
      # restore (including the runfile scripts' own restore) fails with NU1301 when it is missing.
      - name: Ensure local NuGet feed folder
        run: New-Item -ItemType Directory -Force -Path artifacts/packages | Out-Null
        working-directory: ${{ github.workspace }}

      - name: Clean solution
        run: dotnet run --file ./scripts/clean.cs
        working-directory: ${{ github.workspace }}

      - name: Build solution
        run: dotnet run --file ./scripts/build.cs
        working-directory: ${{ github.workspace }}

      - name: Run tests
        run: dotnet run --file ./scripts/test.cs
        working-directory: ${{ github.workspace }}

      - name: Run E2E tests
        run: dotnet run --file ./scripts/e2e.cs
        working-directory: ${{ github.workspace }}
        env:
          UseHttp: "true"

  # Documentation publishing job
  docs:
    if: github.event_name == 'push' && github.ref == 'refs/heads/master' && contains(github.event.head_commit.modified, 'Documentation/')
    runs-on: windows-latest
    defaults:
      run:
        shell: pwsh
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    concurrency:
      group: "pages"
      cancel-in-progress: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Setup DocFX
        run: dotnet tool update --global docfx

      - name: Ensure local NuGet feed folder
        run: New-Item -ItemType Directory -Force -Path artifacts/packages | Out-Null
        working-directory: ${{ github.workspace }}

      - name: Build solution
        run: dotnet run --file ./scripts/build.cs
        working-directory: ${{ github.workspace }}

      - name: Build documentation
        working-directory: Documentation
        run: docfx docfx.json

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload documentation
        uses: actions/upload-pages-artifact@v3
        with:
          path: './Documentation/_site'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # Release publishing job
  release:
    if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.mode == 'release' && inputs.confirm == 'release') || (github.event_name == 'workflow_dispatch' && inputs.mode == 'probe')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write   # Required for NuGet Trusted Publishing (OIDC)
    defaults:
      run:
        shell: pwsh
    steps:
      - name: Validate break-glass confirmation
        if: github.event_name == 'workflow_dispatch' && inputs.mode == 'release' && inputs.confirm != 'release'
        shell: bash
        run: |
          echo "::error::mode=release requires confirm=release. Refusing to run a break-glass release."
          exit 1

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Ensure local NuGet feed folder
        run: New-Item -ItemType Directory -Force -Path artifacts/packages | Out-Null
        working-directory: ${{ github.workspace }}

      - name: Extract version from source/Directory.Build.props
        if: github.event_name != 'workflow_dispatch' || inputs.mode != 'probe'
        id: extract_version
        run: |
          [xml]$xml = Get-Content -Path "source/Directory.Build.props"
          $version = $xml.SelectSingleNode("//Version").InnerText
          $version = $version.Trim()
          echo "version=$version" >> $env:GITHUB_OUTPUT
        shell: pwsh

      - name: Validate release version matches tag
        if: github.event_name == 'release'
        run: |
          $releaseVersion = "${{ steps.extract_version.outputs.version }}"
          $tagName = "${{ github.event.release.tag_name }}"
          $tagNameForComparison = $tagName
          if ($tagName.StartsWith("v")) {
            $tagNameForComparison = $tagName.Substring(1)
          }

          if ($releaseVersion -ne $tagNameForComparison) {
            throw "Release version ($releaseVersion) does not match tag name ($tagName)"
          }
          Write-Host "✅ Release version matches tag name"
        shell: pwsh

      - name: Create NuGet packages
        if: github.event_name != 'workflow_dispatch' || inputs.mode != 'probe'
        run: dotnet run --file ./scripts/package.cs
        working-directory: ${{ github.workspace }}

      - name: NuGet login (OIDC Trusted Publishing)
        if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.mode == 'release' && inputs.confirm == 'release') || (github.event_name == 'workflow_dispatch' && inputs.mode == 'probe')
        id: nuget-login
        uses: nuget/login@v1
        with:
          user: TimeWarp.Enterprises

      - name: Trusted publishing probe result
        if: github.event_name == 'workflow_dispatch' && inputs.mode == 'probe'
        run: echo "Trusted publishing OK — OIDC exchange succeeded for this repo + workflow.yml."

      - name: Publish TimeWarp.State to NuGet
        if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.mode == 'release' && inputs.confirm == 'release')
        run: |
          $packagePath = "./artifacts/packages/TimeWarp.State.*.nupkg"
          if (!(Test-Path $packagePath)) {
            throw "NuGet package not found for TimeWarp.State"
          }
          dotnet nuget push $packagePath --skip-duplicate --source https://api.nuget.org/v3/index.json --api-key ${{ steps.nuget-login.outputs.NUGET_API_KEY }}
        working-directory: ${{ github.workspace }}

      - name: Publish TimeWarp.State.Plus to NuGet
        if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.mode == 'release' && inputs.confirm == 'release')
        run: |
          $packagePath = "./artifacts/packages/TimeWarp.State.Plus.*.nupkg"
          if (!(Test-Path $packagePath)) {
            throw "NuGet package not found for TimeWarp.State.Plus"
          }
          dotnet nuget push $packagePath --skip-duplicate --source https://api.nuget.org/v3/index.json --api-key ${{ steps.nuget-login.outputs.NUGET_API_KEY }}
        working-directory: ${{ github.workspace }}

      - name: Publish TimeWarp.State.Policies to NuGet
        if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.mode == 'release' && inputs.confirm == 'release')
        run: |
          $packagePath = "./artifacts/packages/TimeWarp.State.Policies.*.nupkg"
          if (!(Test-Path $packagePath)) {
            throw "NuGet package not found for TimeWarp.State.Policies"
          }
          dotnet nuget push $packagePath --skip-duplicate --source https://api.nuget.org/v3/index.json --api-key ${{ steps.nuget-login.outputs.NUGET_API_KEY }}
        working-directory: ${{ github.workspace }}
